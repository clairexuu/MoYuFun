# Domain docs

This is a single-context repository.

Before working, read these when present:

- `CONTEXT.md`
- Relevant ADRs in `docs/adr/`

Use terminology from `CONTEXT.md` and flag conflicts with existing ADRs.
