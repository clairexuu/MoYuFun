# MoYuFun 指标与测试口径

本文记录数据与统计里程碑的当前事实、已确定约定和待确认决策。数据库结构以 `supabase/migrations/` 为准，执行状态以 [TODO.md](TODO.md) 为准。

> 当前状态：数据基础结构已完成；测试周期、产品判断阈值和六项核心指标的最终计算口径尚未确定，因此 TODO 第 1 项仍未完成。

## 1. 已完成

### 1.1 数据库基础

Supabase 生产项目已经部署以下表：

- `games`：游戏名称、长短介绍、标签、操作说明、封面、排序和当前版本。
- `game_versions`：不可变版本标识、游戏入口、文件大小和版本说明。
- `events`：匿名访问、加载和游玩事件。

表之间已经配置唯一约束、外键、查询索引和 RLS。「乱刃」及其当前版本 `v1` 已导入。匿名客户端和普通登录客户端没有直接表权限；主站与发布环境使用不同的最小权限服务端角色。

### 1.2 已有事件类型

数据库当前接受以下七种事件类型：

| 事件 | 预期含义 | 当前状态 |
| --- | --- | --- |
| `page_view` | 访问主站公开页面 | 已建字段和数据库约束，尚未采集 |
| `game_detail_view` | 访问某款游戏详情页 | 已建字段和数据库约束，尚未采集 |
| `game_load` | 开始一次游戏 iframe 加载 | 已要求 `load_id`，尚未采集 |
| `game_ready` | 游戏逻辑已准备完成 | 已要求 `load_id`，尚未采集 |
| `game_start` | 玩家开始一局游戏 | 已要求 `play_id`，尚未采集 |
| `game_end` | 一局游戏结束 | 已要求 `play_id`，尚未采集 |
| `heartbeat` | 页面可见且正在游玩时的活跃心跳 | 已要求 `play_id`，尚未采集 |

这些名称已进入生产数据库，但事件发送方、`/api/events` 和 Game SDK 尚未实现。

### 1.3 已有标识和字段

| 字段 | 当前用途 | 当前状态 |
| --- | --- | --- |
| `event_id` | 单次上报的 UUID，数据库唯一约束用于幂等去重 | 已完成数据库约束 |
| `visitor_id` | 浏览器中的匿名访客标识 | 已有数据库字段，生成和持久化未实现 |
| `session_id` | 一次访问会话标识 | 已有数据库字段，30 分钟切分逻辑未实现 |
| `load_id` | 关联一次 `game_load` 与 `game_ready` | 已有数据库字段和必要性约束 |
| `play_id` | 关联一次开始、心跳与结束 | 已有数据库字段和必要性约束 |
| `game_id` | 关联游戏 | 已有数据库外键 |
| `game_version_id` | 关联实际上报的游戏版本 | 已有复合外键，保证版本属于对应游戏 |
| `occurred_at` | 客户端记录的事件发生时间 | 已有字段，时钟偏差规则未确定 |
| `received_at` | 服务端实际入库时间 | 已由数据库自动生成 |
| `duration_ms` | 加载或游玩相关耗时 | 已有非负约束，具体适用事件未最终确定 |
| `active_seconds` | 单次心跳代表的有效活跃秒数 | 已限制为 0～60，累计规则未最终确定 |
| `path` | 页面路径 | 已有字段，允许路径范围未最终确定 |
| `metadata` | 小型扩展信息 | 已限制为 JSON 对象且不超过 8 KiB，允许键尚未确定 |

### 1.4 TODO 已明确但尚未实现的约定

以下内容已经写入 TODO，可作为实现方向，但还没有通过代码和样例数据验收：

- 匿名访客标识需要持久化。
- 连续 30 分钟无活动后创建新会话。
- 开始游玩且页面可见时，每 30 秒发送一次心跳。
- 有效游玩的目标定义包含“累计活跃至少 5 分钟”。
- 原始事件计划保留 90 天，日汇总长期保存。
- 指标需要支持按天、游戏和版本查询。

## 2. 尚未完成

### 2.1 测试周期和样本范围

尚未确定：

- MVP 首轮测试开始日期、结束日期和总天数。
- 是否排除团队内部流量、自动化测试和异常流量。
- 产品判断使用全站数据，还是只使用「乱刃」数据。
- 样本不足时是否延长测试，以及最低访客、加载或开始游玩样本量。
- 新版本发布后是否重新开始观察窗口，或按版本分别评估。

### 2.2 产品判断标准

六项指标目前都没有通过／观察／失败阈值，也没有规定多个指标冲突时如何做最终判断。至少需要确定：

- 哪些指标是上线门槛，哪些只是诊断指标。
- 每项指标的目标值、警戒值和最低样本量。
- 使用单日结果、连续多日结果还是整个测试周期结果。
- 是否要求桌面端和移动端分别达标。

### 2.3 六项核心指标的最终口径

| 指标 | 已有数据基础 | 尚待确定 |
| --- | --- | --- |
| 详情访问率 | `page_view`、`game_detail_view`、访客和会话标识 | 分母采用首页访问、游戏曝光还是全站访问；按事件、会话还是访客去重 |
| 开始游玩率 | `game_detail_view`、`game_start`、`play_id` | 分母是详情访客还是详情会话；重复开始是否重复计数；是否要求先出现详情事件 |
| 加载成功率 | `game_load`、`game_ready`、`load_id` | 超时时长；重试是否算新加载；关闭页面和重复 `ready` 如何处理 |
| 加载耗时 P75 | `game_load`、`game_ready`、`occurred_at`、`duration_ms` | 使用客户端时差还是服务端接收时差；异常值和失败加载是否排除；分组的最小样本量 |
| 有效游玩率 | `game_start`、`heartbeat`、`game_end`、`play_id`、`active_seconds` | 分母是所有开始还是加载成功；心跳丢失、重复和后台切换规则；是否允许跨会话累计 |
| 7 日回访率 | `visitor_id`、`session_id`、`occurred_at` | 首访 cohort 定义；回访窗口是第 7 个自然日还是第 1～7 天；回访需访问主站还是再次游玩 |

在以上选择确认前，不应编写正式聚合 SQL，也不应为这些指标承诺具体数值。

### 2.4 采集和数据质量规则

尚未完成：

- `/api/events` 的请求结构、事件级必填字段和允许的 `metadata` 键。
- 单请求和批量请求的事件数及字节上限。
- 客户端时间与服务端时间的最大允许偏差。
- 事件乱序、重复、延迟到达和缺少前置事件的处理方式。
- 基础限流维度与阈值。
- 内部流量、机器人和监控请求的过滤规则。
- 用于核对六项指标的固定样例事件集和期望结果。
- 90 天原始事件清理任务和长期日汇总表。

## 3. 当前完成度判断

| TODO 第 1 项验收内容 | 状态 | 说明 |
| --- | --- | --- |
| 测试周期 | 未完成 | 没有日期、持续时间和最低样本量 |
| 产品判断标准 | 未完成 | 没有目标值、警戒值和决策规则 |
| 六项核心指标口径 | 未完成 | 已列指标和所需事件，但公式与边界条件未确认 |
| 游戏标签、操作说明和封面字段 | 已完成 | 已写入数据库并导入「乱刃」数据 |
| 单次加载和游玩标识 | 数据库部分完成 | `load_id`、`play_id` 已落库，客户端生命周期尚未实现 |

结论：数据结构部分已具备实现基础，但第 1 项整体仍处于进行中。

## 4. 建议的后续顺序

1. 确定首轮测试周期、纳入流量和最低样本量。
2. 逐项确定六个指标的分子、分母、去重单位、时间窗口和异常处理。
3. 设定目标值、警戒值和最终产品判断规则。
4. 据此冻结 `/api/events` 请求协议与每种事件的字段要求。
5. 如需修改已部署结构，新增向前迁移，不修改现有迁移历史。
6. 准备固定样例事件和预期指标，再进入统计接口与聚合查询实现。
